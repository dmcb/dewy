{{#if operator}}
<li class="rule-group-heading">
    <select>
        <option {{#ifEquals operator 'any'}}selected="selected"{{/ifEquals}}>Any</option>
        <option {{#ifEquals operator 'all'}}selected="selected"{{/ifEquals}}>All</option>
        <option {{#ifEquals operator 'none'}}selected="selected"{{/ifEquals}}>None</option>
    </select>
    of the following rules:
</li>
{{/if}}
{{#if rules}}
<ul class="rule-group">
    {{#rules}}
    {{#if operator}}
        {{> filter_tree}}
    {{/if}}
    {{#unless operator}}
    <li class="rule">
        <select class="field">
            {{#@root.fields}}
                <option {{#ifEquals ../field title}}selected="selected"{{/ifEquals}}>{{title}}</option>
            {{/@root.fields}}
        </select>
        <select class="choice">

        <select>
        <input type="text" />
        <div class="buttons">
            <button>
                <span class="glyphicon glyphicon-minus" aria-hidden="true"></span>
            </button>
            <button>
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            </button>
            <button>
                <span class="glyphicon glyphicon-th-list" aria-hidden="true"></span>
            </button>
        </div>
    </li>
    {{/unless}}
    {{/rules}}
</ul>
{{/if}}