<li>
    <a href="" {{#if children}}class="folder open"{{/if}}>{{#if notifications}}<span class="glyphicon glyphicon-envelope" aria-hidden="true"></span> {{/if}}{{title}}</a>
    {{#if children}}
    <ul>
        {{#children}}
            {{> filter_menu_item}}
        {{/children}}
    </ul>
    {{/if}}
</li>
