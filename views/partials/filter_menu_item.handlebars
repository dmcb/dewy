<li>
    <a href="/sites/{{url}}" class="{{#if children}}folder open{{/if}} {{#ifEquals @root.current_filter.url url}}selected{{/ifEquals}}">{{#if notifications}}<span class="glyphicon glyphicon-envelope" aria-hidden="true"></span> {{/if}}{{title}}</a>
    {{#if children}}
    <ul>
        {{#children}}
            {{> filter_menu_item}}
        {{/children}}
    </ul>
    {{/if}}
</li>
